// Memuat data notes yang disimpan dalam bentuk objek.
const notes = {
    id: '', // Tipe data string untuk id
    title: '', // Tipe data string untuk title
    createdAt: '', // Tipe data string untuk createdAt
    updatedAt: '', // Tipe data string untuk updatedAt
    tags: '', // Tipe data array yang berisi string untuk tags
    body: '', // Tipe data string untuk body
};

module.exports = notes;
